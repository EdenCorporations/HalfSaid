# Migrations are the ONLY source of truth for schema (SPEC §4.3, PRD §24.4).
# Create them with `supabase migration new <name>`. Never edit schema manually.
# The bi-temporal pcg_nodes / pcg_edges schema + RLS policies land in Phase 2.
