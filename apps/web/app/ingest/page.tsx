import { IngestChat } from '@/components/ingest/IngestChat';

/** Teach HalfSaid — chat-only graph building (nothing here is spoken aloud). */
export default function IngestPage() {
  return <IngestChat />;
}
