import { ConversationCanvas } from '@/components/canvas/ConversationCanvas';

/** Screen 1 — Conversation Canvas (SPEC §13). */
export default function CanvasPage() {
  return <ConversationCanvas />;
}
