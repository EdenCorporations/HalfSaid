{
  "name": "@halfsaid/api",
  "version": "0.0.0",
  "private": true,
  "description": "Framework-agnostic /v1 request handlers (auth + RLS-scoped executor injected).",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@halfsaid/retrieval": "*",
    "@halfsaid/safety-policy": "*",
    "@halfsaid/shared-types": "*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "typescript": "^5.6.3"
  }
}
