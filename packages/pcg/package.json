{
  "name": "@halfsaid/pcg",
  "version": "0.0.0",
  "private": true,
  "description": "Personal Communication Graph data layer — migrations, seed, and RLS/schema tests.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing/harness.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit",
    "seed:generate": "node ../../supabase/seed/generate.mjs"
  },
  "dependencies": {
    "@halfsaid/shared-types": "*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "typescript": "^5.6.3"
  }
}
