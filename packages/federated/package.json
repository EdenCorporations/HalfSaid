{
  "name": "@halfsaid/federated",
  "version": "0.0.0",
  "private": true,
  "description": "Federated, privacy-preserving learning of the communication model — DP + secure aggregation, no raw PCG leaves a clinic.",
  "license": "UNLICENSED",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "build": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@halfsaid/retrieval": "*",
    "@halfsaid/shared-types": "*"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
