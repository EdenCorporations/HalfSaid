# React 19 is ahead of some ecosystem peer ranges (e.g. Radix primitives); accept
# the newer React without ERESOLVE failures. Keeps local install and CI `npm ci`
# reproducible from the same lockfile.
legacy-peer-deps=true
save-exact=false
